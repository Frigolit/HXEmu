#ifndef HXEMU_STATEFUL_H
#define HXEMU_STATEFUL_H

// This will be used to define a standard interface for saving and restoring the internal state of
// some classes, to allow saving the state to disk and loading it on startup, simulating the
// battery-backed memory on the real hardware.

class CStateful {
	// TODO: Define this interface
};

#endif
